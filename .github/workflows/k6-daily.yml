name: Daily K6 Load Test

on:
  schedule:
    - cron: '0 6 * * *'  # Her gün sabah 09:00 (UTC+3)
  workflow_dispatch:

jobs:
  run-k6-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install k6
        run: |
          curl -s https://dl.k6.io/key.gpg | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/k6.gpg
          echo "deb https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt update
          sudo apt install k6

      - name: Login to K6 Cloud
        run: k6 login cloud --token "${{ secrets.K6_CLOUD_TOKEN }}"

      - name: Run K6 test in cloud
        run: k6 cloud getir-dashboard-ramping.js

      - name: Send success email
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "✅ Daily K6 Load Test Passed"
          to: ${{ secrets.EMAIL_RECEIVER }}
          from: GitHub Actions <${{ secrets.EMAIL_USERNAME }}>
          body: |
            Merhaba 👋

            Günlük K6 yük testi başarıyla tamamlandı.

            📊 Test Raporu (Grafana):  
            https://ibrahimcemgarip.grafana.net/a/k6-app/runs

            Selamlar,
            GitHub Actions
